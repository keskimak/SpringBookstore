package com.example.NooraBookstore.domain;

import java.util.ArrayList;
import java.util.List;

public class Cart {
	
	//This will contain the books added to the shopping cart. It will hold list of the books and the price of the order. 
	//Customer makes an order which will register the customer id, order number, list of products
	//Make some kind of button onClick(item.add itemList)
	private List<Book> itemList = new ArrayList<Book>();

}
